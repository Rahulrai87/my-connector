// src/connector/utils/cron.util.ts
import { CronJob } from 'cron';

export function isValidCron(expr: string): boolean {
  if (!expr || typeof expr !== 'string') return false;

  try {
    new CronJob(expr, () => {});
    return true;
  } catch {
    return false;
  }
}
