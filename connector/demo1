@Post(':id/run')
async runConnector(@Param('id') connectorId: string) {
const connector = await this.connectorModel.findById(connectorId);


if (!connector) {
throw new NotFoundException('Connector not found');
}


return this.graphService.runSync(
connector._id.toString(),
connector.siteUrl,
);
}
